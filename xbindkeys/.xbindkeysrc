#lala
"xbindkeys_show" 
  control+shift + q

#rofi-pass
"rofi-pass"
  control+F3

#shutdown
"bash ~/private/dotfiles/polybar/polybar/hack/scripts/powermenu.sh"
   alt+F3

#keymap
"setxkbmap -layout us && xmodmap -e 'clear Lock' -e 'keycode 0x42 = Escape'"
   alt+F12

"xmodmap ~/.config/xkb/Xmodmap"
   shift+F12

# emulate left click 
"xdotool click --delay 5 --repeat 1 1"
	alt+shift+z

#Terminal + tmux
"kitty tmux -c 'tmux attach || tmux'"
   control+Return 

"kitty"
   alt+shift+Return

#Emacs Client
"emacs --with-profile own"
   alt+shift+t

#Emacs Client
"emacsclient -c -s own"
   alt+shift+o

#Restart Emacs
"systemctl restart --user emacs_own && emacsclient -c -s own"
   alt+control+o

#Restart Emacs
"systemctl restart --user emacs && emacsclient -c -s doom"
   alt+control+t

##ncmpcpp
#"~/Scripts/launchMusic.sh"
#   alt+shift+m

#nvim
"kitty --title nvim --class nvim -c ~/.config/kitty/vitty.conf tmux -u new-session -s Neovim -A  nvim & bspc desktop -f 1"
   alt+shift+n

##IDE
#"intellij-idea-ultimate-edition& bspc desktop -f 6"
#   alt+shift+s

#PCmanFm
"thunar& bspc desktop -f 5"
   alt + shift + a

#Calibre
"calibre& bspc desktop -f 5"
   alt+shift+i

#firefox
"firefox & bspc desktop -f 3"
   alt+shift+h

#libreoffice
#"libreoffice & bspc desktop -f 5"
#   alt+shift+o


#start retroarch
"retroarch & bspc desktop -f 6"
   alt + shift + w

#Shutdown dmenu
"bash ~/.config/polybar/hack/scripts/powermenu.sh"
   alt + shift + XF86AudioMute

"bash ~/.config/polybar/hack/scripts/powermenu.sh"
   XF86PowerOff

#Monitor dmenu
"bash ~/Scripts/Screens"
   control+alt+s

# Mute volume
"pactl set-sink-mute @DEFAULT_SINK@ toggle"
   XF86AudioMute

# Play/Pause Playerctl(Firefox etc.) + mpd
"
"playerctl play-pause"
	XF86AudioPlay

"playerctl play-pause"
	XF86AudioPause

# Play/Pause Playerctl(Spotifly etc.) + mpd
"playerctl  --player=spotify play-pause & mpc toggle "
	alt+p

# Play/Pause Playerctl(Spotifly etc.) + mpd
"playerctl  --player=spotify previous"
	alt+shift+Left

# Play/Pause Playerctl(Spotifly etc.) + mpd
"playerctl  --player=spotify next"
	alt+shift+Right

# Play/Pause Playerctl(Firefox etc.)
"playerctl play-pause"
	alt+shift+p

# Play/Pause Playerctl(Firefox etc.)
"playerctl play-pause"
	shift + XF86AudioPlay

# Play/Pause mpd
"mpc toggle "
	alt+control+p

# Play/Pause mpd
"mpc toggle "
	shift+XF86AudioPlay

# Play Audio
#bash ~/Scripts/Cmus/PlayPause"
#	Mod2+XF86AudioPlay

# Increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +5%"
   control + XF86AudioRaiseVolume

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -5%"
   control + XF86AudioLowerVolume

   
# Increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +1%"
   control + alt + Up

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -1%"
   control + alt + Down

# Increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +5%"
   control + Up

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -5%"
   control + Down

# restart scc-daemon
"scc-daemon stop && scc-daemon start"
   alt + F1

# Decrease volume
""
   XF86AudioLowerVolume

"networkmanager_dmenu"
   alt + F4
## Increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +5%"
   Mod2 + KP_Subtract + XF86AudioRaiseVolume
#
## Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -5%"
   Mod2 + KP_Subtract  + XF86AudioLowerVolume


#  Next Background
"python ~/Scripts/Background/RandomBg.py randombg"
	alt+XF86AudioNext

#  Prev Background
"python ~/Scripts/Background/RandomBg.py prevbg"
	alt+XF86AudioPrev

#  Prev Background
"python ~/Scripts/Background/RandomBg.py delbg"
	alt+XF86AudioPlay

# Cmus Play Next 
"bash ~/Scripts/Cmus/PlayNext"
	XF86AudioNext

# Cmus Play Prev
"bash ~/Scripts/Cmus/PlayPrev"
	XF86AudioPrev

# Brightnis Up
"sudo light -A 1"
	XF86MonBrightnessUp
  
# Brightnis Up a little
"sudo light -A .2 "
   alt + XF86MonBrightnessUp
   

# Brightnis Down
"sudo light -U 1"
	XF86MonBrightnessDown
  
# Brightnis Down a little
"sudo light -U .2"
	alt +XF86MonBrightnessDown

"xclip -o"
   alt + shift + l

"scrot -s ~/foo.png && xclip ~/foo.png && rm ~/foo.png"
	alt + F2

"scrot '%Y-%m-%d-%m-%s_$p.png' -s -e 'mv $f ~/private/pictures/screenshots/'"
	control + F2

"scrot '%Y-%m-%d-%m-%s_$p.png' -u -e 'mv $f ~/private/pictures/screenshots/'"
	shift + F2



#change KbLayout
"setxkbmap -layout us && xmodmap ~/.Xmodmap"
   control + XF86AudioNext

"setxkbmap -layout dvp && xmodmap ~/.Xmodmap"
   control + XF86AudioPrev


"sh ~/private/development/scripts/touchpad.sh"
   space + control


#"xterm -geom 50x20+20+20"
#   Shift+Mod2+alt + s
#
## set directly keycode (here control+alt+mod2 + f with my keyboard)
#"xterm"
#  alt + c:0x29 + m:4 + mod2
#
## Control+Shift+a  release event starts rxvt
#"rxvt"
#  release+control+shift + a
#
## Control + mouse button 2 release event starts rxvt
#"rxvt"
#  Control + b:2 + Release

##################################
# End of xbindkeys configuration #
##################################
